import Form from "@/components/Forms/addForm";
import React from "react";

const page = () => {
  return (
    <div>
      <h2>Add application :</h2>
      <Form />
    </div>
  );
};

export default page;
